const u = (e) => {
  const t = typeof e;
  return t !== "function" && t !== "object" || e === null;
}, a = (e) => {
  const t = e.flags === "" ? void 0 : e.flags;
  return new RegExp(e.source, t);
}, l = (e, t = /* @__PURE__ */ new WeakMap()) => {
  if (e === null || u(e))
    return e;
  if (t.has(e))
    return t.get(e);
  if (e instanceof RegExp)
    return a(e);
  if (e instanceof Date)
    return new Date(e.getTime());
  if (e instanceof Function)
    return e;
  if (e instanceof Map) {
    const r = /* @__PURE__ */ new Map();
    return t.set(e, r), e.forEach((o, c) => {
      r.set(c, l(o, t));
    }), r;
  }
  if (e instanceof Set) {
    const r = /* @__PURE__ */ new Set();
    t.set(e, r);
    for (const o of e)
      r.add(l(o, t));
    return r;
  }
  if (Array.isArray(e)) {
    const r = [];
    return t.set(e, r), e.forEach((o) => {
      r.push(l(o, t));
    }), r;
  }
  const n = {};
  t.set(e, n);
  for (const r in e)
    Object.prototype.hasOwnProperty.call(e, r) && (n[r] = l(e[r], t));
  return n;
}, f = (e, t = 200) => {
  let n = 0;
  return (...r) => new Promise((o) => {
    n && (clearTimeout(n), o("cancel")), n = window.setTimeout(() => {
      e.apply(void 0, r), n = 0, o("done");
    }, t);
  });
}, p = (e, t = {
  _blank: !0,
  nofollow: !0
}) => {
  const n = document.createElement("a");
  n.href = e, t._blank && (n.target = "_blank"), t.nofollow && (n.rel = "noopener noreferrer"), n.click();
}, d = (e, t = "") => {
  const n = document.createElement("a");
  n.setAttribute("href", e), n.setAttribute("download", t), n.click();
}, m = (e, t, n, r = 100) => {
  let o = e.scrollTop;
  const c = () => {
    const s = t - o;
    o = o + s / 5, Math.abs(s) < 1 ? (e.scrollTo(0, t), n && (typeof r == "number" ? setTimeout(n, r) : n())) : (e.scrollTo(0, o), requestAnimationFrame(c));
  };
  c();
}, w = (e, t = 200) => {
  let n = 0, r = null;
  return (...o) => {
    const c = (s) => {
      n === 0 && (n = s), s - n >= t ? (e.apply(void 0, r), r = null, n = 0) : window.requestAnimationFrame(c);
    };
    r === null && window.requestAnimationFrame(c), r = o;
  };
}, y = (e, t = !0) => {
  if (!e)
    return {};
  const n = {};
  return e.replace(/^\?/, "").split("&").forEach((r) => {
    const [o, c] = r.split("=");
    if (o) {
      const s = t ? decodeURIComponent(o) : o, i = t ? decodeURIComponent(c) : c;
      n[s] !== void 0 ? (Array.isArray(n[s]) || (n[s] = [n[s]]), n[s].push(i)) : n[s] = i;
    }
  }), n;
}, A = (e, t = !0) => e ? Object.keys(e).map((n) => {
  const r = t ? encodeURIComponent(n) : n;
  if (Array.isArray(e[n]))
    return e[n].map((o) => {
      const c = t ? encodeURIComponent(o) : o;
      return `${r}=${c}`;
    }).join("&");
  {
    const o = t ? encodeURIComponent(e[n]) : e[n];
    return `${r}=${o}`;
  }
}).join("&") : "", T = (e, t, n = "asc") => e.sort((r, o) => n === "asc" ? t(r) - t(o) : t(o) - t(r));
export {
  f as debounce,
  l as deepClone,
  d as download,
  u as isPrimitive,
  p as linkTo,
  A as objToSearch,
  T as objectSort,
  y as searchToObj,
  m as smoothScroll,
  w as throttle
};
