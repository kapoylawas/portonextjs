import { ContentPreviewProps } from '../ContentPreview';
/**
 * 注册katex扩展到页面
 *
 */
declare const useMermaid: (props: ContentPreviewProps) => {
    mermaidRef: import("react").MutableRefObject<any>;
    reRender: boolean;
    replaceMermaid: () => void;
};
export default useMermaid;
