export interface HoverData {
    x: number;
    y: number;
}
interface TableShapeProps {
    tableShape: [number, number];
    onSelected: (data: HoverData) => void;
}
declare const TableShape: (props: TableShapeProps) => JSX.Element;
export default TableShape;
